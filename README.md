# CensusData-HadoopMapReduce
A simple Resource allocation algorithm to assign tasks to different Data centers to process big data. A open data set of Census data is processed using Map Reduce Framework.

Developed in Java
